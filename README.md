# Raghav Gupta — portfolio

An Astro static portfolio with an original Blender sculpture, an eight-second exported story animation, five chapter poses, and responsive rendered fallbacks. Existing site content is preserved from commit `c748e5006bfe977709b16b3053315af5a41183e1`; the historical source is in `docs/original-portfolio.html`.

## Run

Requires Node.js 24 and npm.

```sh
npm ci
npm run dev
npm test
npm run build
npm run preview
```

The production build is `dist/`. Public text is in `src/data/portfolio.ts` and `src/pages/index.astro`. Animation behavior is in `src/scripts/main.ts`, `scene.ts`, and the tested chapter mapping in `story.ts`. Design decisions are in `docs/direction.md`; the image-generated composition board is in `docs/design/concept-board.png`.

## Blender asset

See `docs/blender.md` for the exact reproduction command and source file. The original builder and validation scripts are in `scripts/`; editable scenes, storyboard and render evidence are in `assets/blender/`. Website GLB is `public/models/portfolio.glb`. It has one `Story` clip, eight seconds, with chapter times 0/2/4/6/8. No remote modeling service is required.

## Final delivery

The complete website includes Overview, Experience, Selected Projects, Technical Background, and Contact. The original asymmetric orbital sculpture is authored in Blender and loaded as real geometry at the center of the composition. Scatter parts expands its 14 assemblies over nine seconds; Play sequence moves through separation and reassembly over 32 seconds. Thermal and surface-scan effects have been removed.

A local deployment archive can be generated from `dist/` after `npm run build`. The delivered workspace includes `release/portfolio-site.zip`; this generated archive is excluded from Git. The static build includes the missing-page route, search metadata, assets, fonts, and license notices. Editable website and Blender sources remain in this repository. See `docs/validation.md` for measured checks and limitations.

## Behavior

- Desktop uses an on-demand Three.js scene. Native scrolling maps to chapter poses; links can jump directly to any section.
- Mouse-equipped narrow windows retain a live 3D hero. Narrow touch-only devices use rendered imagery.
- OS reduced motion takes precedence. A user motion toggle additionally disables WebGL, text reveals and pointer effects.
- A rendered poster remains if WebGL or the asset fails. Content is prerendered HTML and navigation works without JavaScript.
- Illustrative project diagrams do not perform live inference or request camera access.
- No analytics, accounts, backend, or public API keys.

## Deployment

The configured canonical URL is `https://rgupta100.github.io/`. A GitHub Actions workflow builds and deploys on main/master pushes or manual dispatch. The repository Pages source must be GitHub Actions. The workflow tests and builds the static site before deploying the generated artifact. To publish an update, push the reviewed source to `main`, or run the workflow manually.

## Licenses and provenance

Manrope and IBM Plex Mono are self-hosted through Fontsource; their license files are retained under `public/licenses/`. Three.js and Astro use MIT; GSAP runtime uses its own standard license. The sculpture is generated by this project's original Python builder. The concept board was generated with the built-in image tool and is a design reference, never a substituted production 3D asset. Referenced skill repositories are documented in `docs/tooling.md`.

## Review limits

Biographical claims are preserved from the user's existing public site, not independently certified. Verify co-op availability before publication. Automated performance budgets cover file sizes; real-device frame performance and network speed vary.
