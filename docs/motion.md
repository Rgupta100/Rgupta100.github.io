# Dimensional sculpture sequence

The user's latest direction asks for an Oryzo-inspired experience with convincing depth and individual parts expanding away from the machine.

The focal sequence begins with a short perspective-camera reveal. Scrolling or Play sequence takes the same Blender Story clip through staged separation, radial expansion, ordered inspection and reassembly. Camera azimuth, elevation and distance change with the clip; one controller owns its time. Moving light and selective real-time shadows expose material and depth. Drag and arrow keys rotate the viewpoint without altering animation time.

Narrow mouse-equipped windows use a larger live hero viewport. Touch-only narrow devices and reduced-motion settings retain the rendered fallback. No continuous idle spinning. Rendering sleeps once motion settles, when the document is hidden, and when the narrow hero is offscreen.

Repeated static hardware is batched after loading within its existing animated parent. Animation-target meshes are excluded. This avoids the previously observed Blender GPU-instancing export issue while reducing draw calls.

Reference inspected in browser: https://oryzo.ai/ — large object presentation, changing viewpoints, material detail and a sequence of scene compositions. No reference assets or code copied.
## Thermal and scattered material study
The focal moment is now an asymmetric 38-family constellation. Manual Scatter parts and Play sequence open the whole hero canvas; the introduction becomes inert and fades during inspection. Escape, Return to overview, scrolling, or reassembly restores the reading layout. Navigation remains accessible.

The same real geometry carries an illustrative false-color thermal shader, a traveling cyan scan, and a dark edge-lit inspection phase. The thermal effect runs automatically early in the story; its accessible toggle follows the visible state and supports manual override. Colors are not measured temperatures. Scan surface and sculpture clicks run one 2.6-second scan. No new render loop runs after effects settle.

Play sequence lasts 18 seconds; manual scatter takes 4.8 seconds. Desktop uses the full-width authored Blender pose. Portrait inspection recomposes family centers while preserving mesh proportions and restoring authored transforms before every mixer evaluation. Normal document reading keeps the sculpture in the left visual column. Reduced motion and touch-only narrow sessions keep rendered imagery.
