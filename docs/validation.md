# Validation record — 22 September 2026

## Passed
- Astro check: 0 errors, 0 warnings, 0 hints across 9 files.
- Production static build completes.
- Seven Node tests: chapter mapping (holds, jumps, resize, bounds), actual GLB clip/group contract, image budgets, required destinations and fallback content.
- Revised quantum-inspired Blender sculpture: source reopen and fresh GLB import pass; single Story clip exactly 8 seconds; 214,356 triangles across 1,235 mesh objects; 1,306,944 bytes. Five thermal stages and 128 modeled coaxial lines.
- All 10 chapter images below 250KB each. Desktop overview 88,104 bytes; mobile overview 62,656 bytes.
- Browser DOM and visual checks: 390px mobile, 1280px and 1440px desktop. No horizontal overflow at these widths.
- Actual production WebGL initializes and changes pose at project anchor. Motion off returns to corresponding rendered poster; smooth scrolling is also disabled.
- Mobile menu opens/closes and follows project link. Both diagrams change selected explanation. Copy email reports successful copy.
- Impeccable mechanical detector returned no findings.
- Independent copy audit: Ready after fixing system-reduced-motion control labeling. Optional 'expected' clarification on 2030 education date remains a suggestion, not a factual correction.
- Independent finish review cleared revised quantum hero: final live desktop hero, live exploded project pose and mobile fallback composition. Differentiated gold/nickel/copper materials, structured complexity and text clearance verified.
- Revised alt text reviewed independently: conceptual illustration wording makes no quantum-hardware expertise claim.
- GPU instancing experiment produced invalid animation target indices and a browser load failure. Reverted to reliable ordinary mesh export. Added node-index regression assertions: they fail against the broken export and pass against the shipped one. Restored WebGL is-ready state and live chapter changes verified in browser.

## Limits
- Full-page IAB screenshot stitching duplicated regions even with smooth scroll disabled; these two long captures are not valid layout evidence. Use the named viewport captures instead.
- OS-level reduced-motion behavior was checked in source, not through changing the user's system setting. Motion-off fallback was exercised in browser.
- No claim of physical-device frame-rate or Lighthouse results. The detailed new model has more mesh draw calls than the original frames; lower-end desktop GPU performance remains unmeasured. The lazy desktop Three.js bundle produces a Vite >500KB raw-chunk advisory; it is dynamically imported only on eligible desktop/motion-enabled sessions. Mobile uses stills.
- A real WebGL import failure occurred during dev optimizer restarts; the poster and content remained available. The production bundle subsequently loaded successfully. Context loss recovery is implemented but was not forcibly exercised.
- Public URLs match original site source. Devpost and the GitHub project returned HTTP 200. LinkedIn returned HTTP 405 to a HEAD request, so its live availability remains unverified.
- No publishing, push, or deployment performed.

## Dimensional motion revision — 23 September 2026
- 38 independently animated families, 80 transform channels, one 8-second Blender Story clip; GLB 1,493,124 bytes. Source/import have 1,105 meshes after joining unique cables. 214,356 triangles. All ten fallbacks regenerated; largest 122,190 bytes.
- Perspective camera changes azimuth, elevation and distance across staged separation, radial expansion, inspection and reassembly. Manual playback takes 14 seconds; expansion/reassembly takes four seconds.
- Browser verified: live WebGL, pose 0→2 on Expand assembly, sequence progression, keyboard rotation control, narrow 550px live scene, motion-off hides controls and returns poster. Live renderer reported 190 draw calls after static hardware batching; this is not an FPS measurement.
- Nine Node tests pass, including batching transform preservation and camera path. Astro check 12 files clean; production build passes. Lazy scene bundle retains the Vite size advisory.
- Independent control-copy audit found two minor mismatches: pause actually restarted, and touch dragging was unsupported. Fixed with Stop/Replay labels and mouse-specific instructions.
- Full 14-second playback reaches pose 4.000 and Replay sequence appears. Final expansion framing pulls back to distance 23; desktop controls remain fixed below the model and hide after 80px of desktop scrolling.
- Final viewport captures verify expanded model/control clearance and partial-scroll text clearance; controls hide after 80px desktop scroll. Independent control-copy review cleared both fixes.


## Thermal / scatter revision — 23 September 2026
- Blender scatter spans 13.585 × 6.646 × 4.703 units with asymmetric positions and rotations. 38 families, 8-second Story; GLB 1,594,532 bytes. Source reopened and GLB freshly imported. All ten chapter fallback images regenerated, each under 110KB.
- Desktop manual inspection opens the full hero canvas; portrait inspection recomposes family centers without scaling mesh proportions. Escape and return restore reading. Existing reduced-motion and narrow touch-only fallbacks retained.
- Thermal shader compiled in the browser with no reported errors. Illustrative legend, accessible temperature disclaimer, manual thermal toggle and automatic shader sequence verified. Surface scan adds a bounded 2.6-second cyan sweep and edge-lit material phase.
- Nine existing tests pass. Astro check 13 files clean; production build passes with existing lazy scene bundle-size advisory. No FPS or physical-device performance claim.
- Independent content audit found two minor issues (accessible thermal context, toggle state during autoplay); both resolved and cleared.
- Final full-playback check reached pose 4.000 / Replay sequence with hero text restored and no console errors. Motion-off during scatter hides controls, restores text and poster. Independent visual review cleared desktop/portrait framing; its sole icon-consistency finding was fixed with the existing SVG arrow.

## Final website delivery — 23 September 2026
- Desktop overview, experience, projects, background and contact reviewed; 390px overview and projects reviewed without horizontal overflow. Independent finish review: ship, no material fixes.
- Final content audit cleared source-consistent biography, attribution, links and effects labeling. Optional expected-graduation clarification applied.
- Added and visually checked branded 404 route; canonical, Open Graph image, robots and sitemap configured.
- Final Astro check: 14 files, zero errors/warnings/hints. Static build and all nine tests pass. Existing lazy Three.js chunk-size advisory remains; physical-device performance not measured.
- Checked 24 local HTML references/anchors across two built pages. Deployment archive release/portfolio-site.zip is 2,029,286 bytes; SHA-256 stored in release/build-manifest.json.
- Website, Blender sources and GitHub Pages workflow complete. No push, publication or deployment performed.
